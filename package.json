{
  "name": "mpmek",
  "version": "1.0.0",
  "description": "Розклад Студента МПМЕК — PWA + serverless API",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "esbuild app/app.js --minify --allow-overwrite --outfile=app/app.js && esbuild app/sw.js --minify --allow-overwrite --outfile=app/sw.js",
    "test": "node tests/run-all.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.97",
    "@simplewebauthn/server": "^11.0.0",
    "@vercel/blob": "^0.27.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
